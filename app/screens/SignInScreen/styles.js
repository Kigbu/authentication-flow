import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    root: {
        alignItems: 'center',
        padding: 20,
    },
    logoWrapper: { paddingVertical: 20 },
    logo: {
        width: '50%',
        maxWidth: 100,
        maxHeight: 100,
    },
});
